#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int x;
    cin >> x;
    if (x % 2 == 0) {
        cout << "Четное";
    }
    else
        cout << "Не четное";
    return 0;  
}
