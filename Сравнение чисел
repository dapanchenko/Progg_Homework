#include <iostream>
#include <stdio.h>

int main()
{
    int x, y;
    std::cin >> x >> y;
    if (x > y) {
        std::cout << 1;
    }
    else if (x <= y) {
        std::cout << 0;
    }
    return 0;
    
}
